# Dependencies
node_modules/

# Python
__pycache__/
*.py[cod]

# Build caches
.rollup.cache/
*.tsbuildinfo

# Logs / OS cruft
*.log
.DS_Store
Thumbs.db

# Release archives (built on demand, not tracked)
*.zip

# Note: dist/ is intentionally committed — it's the installable plugin artifact.
